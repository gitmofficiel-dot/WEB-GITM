const fs = require('fs'); const path = require('path'); function searchFiles(dir, term) { const files = fs.readdirSync(dir); files.forEach(file => { const filePath = path.join(dir, file); if (fs.statSync(filePath).isDirectory()) { searchFiles(filePath, term); } else if (filePath.endsWith('.js') || filePath.endsWith('.jsx')) { const content = fs.readFileSync(filePath, 'utf8'); if (content.includes(term)) console.log(filePath); } }); } searchFiles('src', 'openai');
